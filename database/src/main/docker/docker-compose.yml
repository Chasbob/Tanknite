version: '3.5'

services:
  postgres:
    container_name: 'tanknite'
    image: 'postgres'
    restart: 'always'
    environment:
      POSTGRES_PASSWORD: 'example'
      POSTGRES_USER: 'example'
      POSTGRES_DB: 'tanknite'
    ports:
      - 5432:5432
  adminer:
    container_name: 'adminer'
    image: adminer
    restart: always
    ports:
      - 8080:8080